apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: sample
  name: rails-ing
spec:
  rules:
  - host: testing.saple-docker.jp
    http:
      paths:
      - path: /
        backend:
          serviceName: rails
          servicePort: 3000
